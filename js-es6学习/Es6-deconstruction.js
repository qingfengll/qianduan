// let {disVersion='1'} = {disVersion: null}

// let arr1 = [1,2,3]
// arr1.unshift(4,5);
// console.log(...arr1);
// console.log(disVersion);

let arr = {};

if (Object.keys(arr).length <= 0) {
    console.log(Object.keys(arr).length);
}

